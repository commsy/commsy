; See https://docs.docker.com/desktop/features/networking/networking-how-tos/#connect-a-container-to-a-service-on-the-host
; See https://github.com/docker/for-linux/issues/264
; The `client_host` below may optionally be replaced with `discover_client_host=yes`
; Add `start_with_request=yes` to start debug session on each request
xdebug.client_host = 'host.docker.internal'
; xdebug.start_with_request = yes
; The mode is not set here: XDEBUG_MODE in the environment wins over the ini
; file, so it would be a second, silently ignored source of truth.
