; https://symfony.com/doc/current/performance.html#don-t-check-php-files-timestamps
; The image is immutable, so there is nothing to notice a change in.
opcache.validate_timestamps = 0

; https://symfony.com/doc/current/performance.html#use-the-opcache-class-preloading
; Off for now: preloading pins the legacy classes into every worker, and the
; class map is not yet stable enough for that to be a win.
;opcache.preload_user = www-data
;opcache.preload = /app/config/preload.php
