{% extends 'base/profile.html.twig' %}

{% import 'utils/macros.html.twig' as macros %}

{% block content %}
    <main id="main-content" aria-labelledby="room-profile-heading">
        {% component 'v2:panel' with { name: 'room-profile', style: 'primary', title: 'general'|trans({}, 'menu') } %}
            {% block content %}
                <div class="uk-margin-left uk-margin-right">
                    <twig:ProfileImageForm :userId="user.itemId"/>
                </div>
            {% endblock %}
        {% endcomponent %}
    </main>
{% endblock content %}
