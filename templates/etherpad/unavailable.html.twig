{# Shown in place of the pad when Etherpad cannot be reached or refuses a
   call. Deliberately generic: the cause belongs in the log, not on screen. #}
<div class="uk-alert uk-alert-danger">
    {{ 'etherpad unavailable'|trans({}, 'material') }}
</div>
