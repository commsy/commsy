
{% import 'utils/macros.html.twig' as macros %}

{% if not linkedItemsByRubric is empty %}
    {% set count = 1 %}
        {% for type,rubric in linkedItemsByRubric %}
        {{ type|capitalize|trans({'%count%':1},"rubric") }} ({{ rubric|length }}){% if count < linkedItemsByRubric|length %}, {% endif %}
        {% set count = count +1 %}
    {% endfor %}

{% endif %}
