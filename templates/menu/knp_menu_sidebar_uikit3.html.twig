{% extends 'knp_menu.html.twig' %}

{% block linkElement %}
    {% import _self as knp_menu %}

    {% if not item.getExtra('showList', false) %}
        <a href="{{ item.uri }}"{{ knp_menu.attributes(item.linkAttributes) }}>
            {% if item.extras.icon is defined %}
                <span class="uk-margin-small-right" uk-icon="{{ item.extras.icon }}"></span>
            {% endif %}
            <span class="uk-visible@l">{{ block('label') }}</span>
        </a>
    {% else %}
        {{ block('label') }}
    {% endif %}
{% endblock %}

{% block label %}
    {% if options.allow_safe_labels and item.getExtra('safe_label', false) %}
        {{ item.label|raw }}
     {% else %}
        {{ item.label|trans({'%count%':1}, item.getExtra('translation_domain'))|upper }}
    {% endif %}
{% endblock %}


