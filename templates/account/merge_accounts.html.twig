{% extends 'base/profile.html.twig' %}

{% import 'utils/macros.html.twig' as macros %}

{% form_theme form 'form/uikit_horizontal_layout.html.twig' %}

{% block side_navigation %}
    <nav aria-label="{{ 'Account'|trans({}, 'profile') }}">
    {{ knp_menu_render('account') }}
    </nav>
{% endblock %}

{% block content %}
    {% set panelName = 'account-merge' %}
    <main id="main-content" aria-labelledby="{{ panelName }}-heading">
    {% component 'v2:panel' with { name: panelName, title: 'combineAccount'|trans({}, 'profile') } %}
        {% block content %}
            {{ form_start(form) }}
                {{ form_errors(form) }}

                <table class="uk-table">
                    <tr>
                        <td>{{ 'mergeAccountsTextFirstLine'|trans({},'profile') }}</td>
                    </tr>
                    <tr>
                        <td>{{ 'mergeAccountsTextSecondLine'|trans({},'profile') }}</td>
                    </tr>
                    <tr>
                        <td>{{ form_row(form.combineUserId) }}</td>
                    </tr>
                    <tr>
                        <td>{{ form_row(form.combinePassword) }}</td>
                    </tr>
                    {% if form.auth_source is defined %}
                        <tr>
                            <td>{{ form_row(form.auth_source) }}</td>
                        </tr>
                    {% endif %}
                </table>

                <table class="uk-table">
                    <tr>
                        <td>{{ form_widget(form.save) }}</td>
                    </tr>
                </table>
                {{ form_row(form._token) }}
            {{ form_end(form) }}
        {% endblock %}
    {% endcomponent %}
    </main>
{% endblock content %}
