{{ '[This email was generated automatically at %time_generated%.]'|trans({ '%time_generated%': 'now'|date|craue_datetime('de-DE') }, 'mail') }}

<br/><br/>

{{ 'mail.account_registration.intro'|trans({
    '%fullname%': account.firstname ~ ' ' ~ account.lastname,
    '%username%': account.username
}, 'mail') }}

<br/><br/>

{{ 'mail.user_joined_details'|trans({}, 'mail') }}:<br/>
Name: {{ account.firstname }} {{ account.lastname }}<br/>
Account: {{ account.username }}<br/>
E-Mail: {{ account.email }}

<br/><br/>

{% set url = url('app_helper_portalenter', { 'context': account.contextId }) %}
<a href="{{ url }}">{{ url }}</a>
