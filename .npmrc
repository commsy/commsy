# Install file: dependencies as copies rather than symlinks. As links, npm also
# resolves their devDependencies, which drags a test toolchain into the tree and
# breaks the install outright.
install-links=true
