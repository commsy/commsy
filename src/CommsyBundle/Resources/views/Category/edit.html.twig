{% extends 'CommsyBundle:Base:room.html.twig' %}

{% form_theme newForm 'CommsyBundle:Form:uikit_horizontal_layout.html.twig' %}
{% form_theme editForm 'CommsyBundle:Form:uikit_horizontal_layout.html.twig' %}

{% block content %}
    <div class="uk-grid">

        <div class="uk-width-1-1">

            {# new category #}
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div class="uk-text-large">{{ 'Create new category'|trans({}, "category") }}</div>
                     </div>
                </div>

                <div>
                    <div class="uk-grid">
                        {# form column #}
                        <div class="uk-width-1-3">
                            {{ form_start(newForm) }}
                            {{ form_widget(newForm) }}
                            {{ form_end(newForm) }}
                        </div>
                    </div>
                </div>
                
            </div>

            {# edit category #}
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div class="uk-text-large">{{ 'Edit categories'|trans({}, "category") }}</div>
                     </div>
                </div>

                <div>
                    <div class="uk-grid">
                        {# form column #}
                        <div class="uk-width-1-1">
                            {{ form_start(editForm) }}
                            {{ form_widget(editForm) }}
                            {{ form_end(editForm) }}
                        </div>
                    </div>
                </div>
                
            </div>
        </div>
    </div>
{% endblock %}