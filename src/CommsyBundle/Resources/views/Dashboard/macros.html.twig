{% macro dashboardFeed(roomItem) %}
    <li id="dashboard-feed" class="uk-grid-margin">
        <div class="uk-panel uk-panel-box uk-panel-box-primary">
            <div class="uk-panel-teaser cs-panel-teaser-primary">
                <div class="uk-flex uk-flex-middle">
                    <i class="uk-sortable-handle uk-icon uk-icon-bars uk-margin-small-right"></i>
                    <div class="uk-text-large">{{ 'latest entries from all rooms'|trans({}, "room") }}</div>
                 </div>
            </div>

            <div>
                <ul id="dashboard-feed" class="uk-comment-list">
                    {{ render(controller(
                        'CommsyBundle:Dashboard:feed',
                        { 'itemId': roomItem.itemId }
                    ))}}
                </ul>
                <div class="feed-load-more" data-uk-scrollspy="{cls: 'uk-animation-fade', repeat: true, topoffset: 500}" data-feed='{"url": "{{ app.request.baseURL }}/dashboard/{{ roomItem.itemId }}/feed/", "target": "#dashboard-feed"}'>
                    <i class="uk-icon-refresh uk-icon-spin uk-icon-medium"></i>
                </div>
            </div>
            
        </div>
    </li>
{% endmacro %}

{% macro dashboardCalendar(roomItem) %}
    <li id="dashboard-calendar" class="uk-grid-margin">
        <div class="uk-panel uk-panel-box uk-panel-box-primary">
            <div class="uk-panel-teaser cs-panel-teaser-primary">
                <div class="uk-flex uk-flex-middle">
                    <i class="uk-sortable-handle uk-icon uk-icon-bars uk-margin-small-right"></i>
                    <div class="uk-text-large">{{ 'calendar'|trans({}, "date") }}</div>
                 </div>
            </div>
            <div>
                {{ render(controller(
                    'CommsyBundle:Date:calendardashboard',
                    { 'roomId': roomItem.itemId }
                ))}}
            </div>
        </div>
    </li>
{% endmacro %}

{% macro dashboardTodo1(roomItem) %}
    <li id="dashboard-todo1" class="uk-grid-margin">
        <div class="uk-panel uk-panel-box uk-panel-box-primary">
            <div class="uk-panel-teaser cs-panel-teaser-primary">
                <div class="uk-flex uk-flex-middle">
                    <i class="uk-sortable-handle uk-icon uk-icon-bars uk-margin-small-right"></i>
                    <div class="uk-text-large">{{ '...'|trans({}, "room") }}</div>
                 </div>
            </div>
            <div>
                ...
            </div>
        </div>
    </li>
{% endmacro %}

{% macro dashboardTodo2(roomItem) %}
    <li id="dashboard-todo2" class="uk-grid-margin">
        <div class="uk-panel uk-panel-box uk-panel-box-primary">
            <div class="uk-panel-teaser cs-panel-teaser-primary">
                <div class="uk-flex uk-flex-middle">
                    <i class="uk-sortable-handle uk-icon uk-icon-bars uk-margin-small-right"></i>
                    <div class="uk-text-large">{{ '...'|trans({}, "room") }}</div>
                 </div>
            </div>
            <div>
                ...
            </div>
        </div>
    </li>
{% endmacro %}