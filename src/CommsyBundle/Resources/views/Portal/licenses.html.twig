{% extends 'CommsyBundle:Base:portal.html.twig' %}
{% form_theme form 'CommsyBundle:Form:uikit_horizontal_layout.html.twig' %}

{% block content %}
    <div class="uk-grid" data-uk-grid-margin>
        <div class="uk-width-1-1">
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div class="uk-text-large">{{ 'Edit Licenses'|trans({}, "room") }}</div>
                    </div>
                </div>

                <div>
                    <div class="uk-grid">
                        {# form column #}
                        <div class="uk-width-1-3">
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            {{ form_end(form) }}
                        </div>
                        {# show existing categories #}
                        <div id="licenses" class="uk-width-2-3">
                            {% for license in licenses %}
                                <span>
                                     <a class="uk-button uk-margin-small-bottom uk-margin-small-right{% if licenseId == license.id %} uk-button-primary{% endif %}" href="{{ path('commsy_portal_licenses', { 'roomId': roomId, 'licenseId': license.id }) }}">
                                         <span class="uk-vertical-align-middle">
                                            {{- license.title|trans({},'portal') -}}
                                        </span>
                                    </a>
                                </span>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        function toggleMandatoryLinksCheckbox() {
            $('#roomcategories_link_mandatory').prop('disabled', $("#categories .uk-margin-small-bottom").length < 1);
        }

        window.onload = function() {
            toggleMandatoryLinksCheckbox();
        }
    </script>
{% endblock %}
