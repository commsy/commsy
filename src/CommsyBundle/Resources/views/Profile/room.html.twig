{% extends 'CommsyBundle:Profile:settings.html.twig' %}

{% import 'CommsyBundle:Utils:macros.html.twig' as macros %}

{% form_theme form 'CommsyBundle:Form:uikit_horizontal_layout.html.twig' %}

{% block content %}
    <div class="uk-grid">

        <div class="uk-width-medium-3-4">
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div>{{ 'user profile'|trans({}, 'profile') }}</div>
                    </div>
                </div>
                <div class="uk-margin-left uk-margin-right">
                    {{ form_start(form) }}
                        {{ form_errors(form) }}
                        <div class="uk-panel uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'profile'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(form.firstname) }}</td>
                                    <td rowspan="7">
                                        <div class="uk-clearfix uk-cover-background uk-position-relative">
                                            <span style="width: 200px; position: absolute; text-align: center;">{{ 'profileimage'|trans({},'profile')|capitalize }}</span>
                                            <img id="profile_form_user_image" class="uk-thumbnail uk-float-right uk-margin-bottom uk-border-circle" src="{{ path('commsy_user_image', {'roomId': user.contextId, 'itemId': user.itemId}) }}" data-uk-tooltip title="{{ user.fullname }}">
                                            <div class="uk-position-cover">
                                                {{ form_widget(form.image) }}
                                            </div>
                                        </div>
                                        <div class="uk-float-right uk-clearfix" style="margin-top: 220px; float: none; margin-top: 240px; width: 200px; text-aling: center;">
                                            {{ form_row(form.imageChangeInAllContexts) }}
                                        </div>
                                    </td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.lastname) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.userId) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.currentPassword) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.newPassword) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.newPasswordConfirm) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.language) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.dateOfBirth) }}</td>
                                    <td>{{ form_row(form.dateOfBirthChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.autoSaveStatus) }}</td>
                                </tr>
                            </table>
                        </div>
    
                        <div class="uk-panel uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'personalData'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(form.title) }}</td>
                                    <td>{{ form_row(form.titleChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.street) }}</td>
                                    <td>{{ form_row(form.streetChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.zipcode) }}</td>
                                    <td>{{ form_row(form.zipcodeChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.city) }}</td>
                                    <td>{{ form_row(form.cityChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.room) }}</td>
                                    <td>{{ form_row(form.roomChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.organisation) }}</td>
                                    <td>{{ form_row(form.organisationChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.position) }}</td>
                                    <td>{{ form_row(form.positionChangeInAllContexts) }}</td>
                                </tr>
                            </table>
                        </div>
                        
                        <div class="uk-panel uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'contactData'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(form.email) }}</td>
                                    <td>{{ form_row(form.emailChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.isEmailVisible) }}</td>
                                    <td>{{ form_row(form.isEmailVisibleChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.phone) }}</td>
                                    <td>{{ form_row(form.phoneChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.mobile) }}</td>
                                    <td>{{ form_row(form.mobileChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.icq) }}</td>
                                    <td>{{ form_row(form.icqChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.msn) }}</td>
                                    <td>{{ form_row(form.msnChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.skype) }}</td>
                                    <td>{{ form_row(form.skypeChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.yahoo) }}</td>
                                    <td>{{ form_row(form.yahooChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.jabber) }}</td>
                                    <td>{{ form_row(form.jabberChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.homepage) }}</td>
                                    <td>{{ form_row(form.homepageChangeInAllContexts) }}</td>
                                </tr>
                                <tr>
                                    <td colspan="2">
                                        {{ form_row(form.description) }}
                                        {{ form_row(form.descriptionChangeInAllContexts) }}
                                    </td>
                                </tr>
                            </table>
                        </div>
                        
                        <div class="uk-panel uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'newsletter'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(form.newsletterStatus) }}</td>
                                </tr>
                            </table>
                        </div>
                        
                        <div class="uk-panel uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'commsyBar'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(form.widgetStatus) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.calendarStatus) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.stackStatus) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.portfolioStatus) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(form.switchRoomStatus) }}</td>
                                </tr>
                            </table>
                        </div>
    
                        <div class="uk-panel uk-margin-large-bottom">
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_widget(form.save) }}</td>
                                </tr>
                            </table>
                        </div>
                    {{ form_end(form) }}
                    
                    {{ form_start(formCombine) }}
                        {{ form_errors(formCombine) }}
                        <div class="uk-panel uk-panel-divider uk-margin-large-bottom">
                            <div class="uk-panel uk-panel-header">
                                <h3>{{ 'combineAccount'|trans({},'profile')|capitalize }}</h3>
                            </div>
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_row(formCombine.combineUserId) }}</td>
                                </tr>
                                <tr>
                                    <td>{{ form_row(formCombine.combinePassword) }}</td>
                                </tr>
                            </table>
                        </div>
                    
                        <div class="uk-panel uk-margin-large-bottom">
                            <table class="uk-table">
                                <tr>
                                    <td>{{ form_widget(formCombine.save) }}</td>
                                </tr>
                            </table>
                        </div>
                    {{ form_end(formCombine) }}
                </div>
            </div>
        </div>
        
        <div class="uk-width-medium-1-4">
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div>{{ 'Information'|trans({}, 'settings') }}</div>
                    </div>
                </div>

                <div>
                    Some usefull text...
                    <canvas id="profile_image_hidden_data" width="200px" height="200px" style="border: 1px solid black;" class="uk-hidden"></canvas>
                </div>
            </div>
        </div>
    </div>

    <script>
    window.onload = function(){

        var positionCover = document.querySelector('.uk-position-cover');
        var coverFormControls = positionCover.getElementsByClassName('uk-form-controls')[0];

        coverFormControls.style.marginLeft = "0px";
        coverFormControls.style.height = "100%";

        document.querySelector('form.uk-form-horizontal').onsubmit = function(event){
            this.querySelector('input[type="file"]').disabled = true;
        }

        var profileUpload = document.querySelector('.cs-upload-form');

        profileUpload.style.height = "100%";
        profileUpload.style.padding = "0";

        profileUpload.onmouseover = function(e){
            e.preventDefault();
            positionCover.style.opacity = "0.7";
            return false;
        };

        profileUpload.onmouseleave = function(e){
            e.preventDefault();
            positionCover.style.opacity = "0.0";
            return false;
        };

        profileUpload.ondragover = function(e){
            e.preventDefault();
            this.style.border = "1px solid black";
            positionCover.style.opacity = "0.7";
            return false;
        };
        profileUpload.ondragleave = function(e){
            e.preventDefault();
            this.style.border = "";
            positionCover.style.opacity = "0.0";
            return false;
        };
        profileUpload.ondragend = function(e){
            e.preventDefault();
            positionCover.style.opacity = "0.0";
            return false;
        };
        profileUpload.ondrop = function(e){
            e.preventDefault();
            this.style.border = "";
            positionCover.style.opacity = "0.0";
            setProfileImage(e.dataTransfer.files[0], document.querySelector('#profile_form_user_image'));
        };

        profileUpload.querySelector('input[type="file"]').onchange = function(e){
            setProfileImage(e.target.files[0], document.querySelector('#profile_form_user_image'));
        };
    };

    function setProfileImage(f, previewImage){
        // TODO: set threshold to sensible value (e.g. the real upload size limit of the server)!
        if(f.size > 2000000){
            alert("File size too large ("+(f.size / 1000) +" KB)! \n This service accepts image files up to 500 KB only!");
            return false;
        }
        else if(f.type.substring(0, 5) != 'image'){
            alert("Files of type '"+f.type+"' can not be used as profile images!");
            return false;
        }

        var reader = new FileReader();
        reader.onload = function(event){
            previewImage.onload = function(e){
                this.onload = null;

                var edgeLength = 200;
                var offset = Math.abs(previewImage.naturalWidth - previewImage.naturalHeight) / 2;
                var minDim = Math.min(previewImage.naturalWidth, previewImage.naturalHeight);

                var x = 0, y = 0;
                if(previewImage.naturalWidth > previewImage.naturalHeight){
                    x = offset;
                }
                else{
                    y = offset;
                }

                var c = document.querySelector("#profile_image_hidden_data");
                var ctx = c.getContext("2d");

                ctx.clearRect(0, 0, edgeLength, edgeLength);
                ctx.drawImage(previewImage, x, y, minDim, minDim, 0, 0, edgeLength, edgeLength);

                previewImage.src = c.toDataURL(f.type);
            }
            previewImage.src = event.target.result;
            document.querySelector('#room_profile_image_data').value = f.name + ";" + event.target.result;
        }
        reader.readAsDataURL(f);
    }
    </script>
{% endblock content %}