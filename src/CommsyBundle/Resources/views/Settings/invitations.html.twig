{% extends 'CommsyBundle:Base:settings.html.twig' %}

{% block content %}
    <div class="uk-grid">
        <div class="uk-width-medium-4-4">
            <div class="uk-panel uk-panel-box uk-panel-box-primary">
                <div class="uk-panel-teaser cs-panel-teaser-primary">
                    <div class="uk-flex uk-flex-middle uk-flex-space-between">
                        <div>{{ 'Invitations'|trans({}, 'settings') }}</div>
                    </div>
                </div>

                <div>
                    <b>{{ 'New invitation'|trans({}, 'settings') }}:</b><br/>
                    {{ form_start(form) }}
                    <div class="uk-panel uk-panel-box" style="margin-bottom: 20px;">
                        <h1 class='uk-panel-title'>{{ 'Invitation'|trans({}, 'settings') }}</h1>
                        <div style="margin-bottom: 25px;">{{ 'Invitation text'|trans({}, 'settings') }}</div>
                        {{ form_widget(form.email) }}
                    </div>
                    <div class="uk-panel uk-panel-box" style="margin-bottom: 20px;">
                        <h1 class='uk-panel-title'>{{ 'Remove invitation'|trans({}, 'settings') }}</h1>
                        <div style="margin-bottom: 25px;">{{ 'Remove invitation text'|trans({}, 'settings') }}</div>
                        {{ form_widget(form.remove_invitees) }}
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock content %}