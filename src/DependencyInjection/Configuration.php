<?php

namespace App\DependencyInjection;

use Symfony\Component\Config\Definition\Builder\TreeBuilder;
use Symfony\Component\Config\Definition\ConfigurationInterface;

/**
 * This is the class that validates and merges configuration from your app/config files
 *
 * To learn more see {@link http://symfony.com/doc/current/cookbook/bundles/extension.html#cookbook-bundles-extension-config-class}
 */
class Configuration implements ConfigurationInterface
{
    /**
     * {@inheritdoc}
     */
    public function getConfigTreeBuilder()
    {
        $treeBuilder = new TreeBuilder();
        $rootNode = $treeBuilder->root('commsy');

        $rootNode
            ->children()
                ->arrayNode('clamscan')
                    ->children()
                        ->booleanNode('virus_scan')
                            ->defaultFalse()
                        ->end()
                        ->booleanNode('virus_use_php')
                            ->defaultFalse()
                        ->end()
                        ->booleanNode('virus_scan_cron')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('virus_scan_path')
                            ->defaultValue('/usr/bin')
                        ->end()
                        ->scalarNode('virus_scan_bin')
                            ->defaultValue('clamscan')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('cookie')
                    ->children()
                        ->scalarNode('domain')
                            ->defaultValue(null)
                        ->end()
                        ->scalarNode('path')
                            ->defaultValue(null)
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('db')
                    ->children()
                        ->scalarNode('backup_prefix')
                            ->defaultValue('zzz')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('email')
                    ->children()
                        ->scalarNode('from')
                            ->defaultValue('example@example.com')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('upload')
                    ->children()
                        ->booleanNode('enabled')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('server')
                            ->defaultValue('')
                        ->end()
                        ->integerNode('port')
                            ->defaultValue(993)
                        ->end()
                        ->scalarNode('options')
                            ->defaultValue('/imap/ssl')
                        ->end()
                        ->scalarNode('account')
                            ->defaultValue('example@example.com')
                        ->end()
                        ->scalarNode('password')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('login')
                    ->children()
                        ->booleanNode('shibboleth_direct_login')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('shibboleth_redirect_url')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('shibboleth_deactivate_direct_login_by_portal_id')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('security')
                    ->children()
                        ->scalarNode('privacy_disable_overwriting')
                            ->defaultFalse()
                        ->end()
                        ->booleanNode('allow_moderator_takeover')
                            ->defaultTrue()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('settings')
                    ->children()
                        ->scalarNode('export_temp_folder')
                            ->defaultValue('var/temp/zip_export')
                        ->end()
                        ->integerNode('delete_days')
                            ->defaultValue(182)
                        ->end()
                        ->integerNode('session_lifetime')
                            ->defaultValue(6)
                        ->end()
                        ->scalarNode('proxy_ip')
                            ->defaultValue(null)
                        ->end()
                        ->scalarNode('proxy_port')
                            ->defaultValue(null)
                        ->end()
                        ->booleanNode('single_cat_selection')
                            ->defaultFalse()
                        ->end()
                        ->booleanNode('item_locking')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('return_path_mail_address')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('themes')
                    ->children()
                        ->scalarNode('default')
                            ->defaultValue('default')
                        ->end()
                        ->scalarNode('cs_date_color_no_color')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_01')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_02')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_03')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_04')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_05')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_06')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_07')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_08')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_09')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('cs_date_color_10')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('wordpress')
                    ->children()
                        ->booleanNode('enabled')
                            ->defaultFalse()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('limesurvey')
                    ->children()
                        ->booleanNode('enabled')
                            ->defaultFalse()
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('etherpad')
                    ->children()
                        ->booleanNode('enabled')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('api_key')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('api_url')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('base_url')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('mediawiki')
                    ->children()
                        ->booleanNode('enabled')
                            ->defaultFalse()
                        ->end()
                        ->scalarNode('url')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('roomWikiUrl')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('apiUrl')
                            ->defaultValue('/api.php')
                        ->end()
                        ->scalarNode('consumerKey')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('consumerSecret')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('accessToken')
                            ->defaultValue('')
                        ->end()
                        ->scalarNode('accessSecret')
                            ->defaultValue('')
                        ->end()
                    ->end()
                ->end()
                ->arrayNode('dates')
                    ->children()
                        ->scalarNode('timezone')
                        ->defaultValue('UTC')
                    ->end()
                ->end()
            ->end()
        ;

        // Here you should define the parameters that are allowed to
        // configure your bundle. See the documentation linked above for
        // more information on that topic.

        return $treeBuilder;
    }
}
