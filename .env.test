# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

PANTHER_APP_ENV=panther
PANTHER_ERROR_SCREENSHOT_DIR=./var/error-screenshots

#DATABASE_URL="mysql://app:!ChangeMe!@db:3306/app_test?serverVersion=11.8.5-MariaDB&charset=utf8mb4"
# Not reachable on purpose: tests must never talk to a search server. A value is
# still needed so the indexing messages are dispatched and can be asserted on.
ELASTICSEARCH_URL=http://elastic.invalid:9200/
API_READ_SECRET=apiread
API_WRITE_SECRET=apiwrite
API_METRICS_SECRET=metricssecret

EMAIL_FROM=test@commsy.net
