api_platform:
    title: CommSy
    description: A web-based community system.
    version: 2.1.0
    swagger:
        api_keys:
            JWT:
                name: Authorization
                type: header
    formats:
        json: ['application/json']
    docs_formats:
        json: ['application/json']
        jsonopenapi: ['application/vnd.openapi+json']
        html: ['text/html']
    error_formats:
        jsonproblem: ['application/problem+json']
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
        extra_properties:
            # Todo: Setting rfc_7807_compliant_errors to 'true' is desirable,
            # but lead into strange behaviour, causing all API test after one,
            # that is expecting a 404 response to fail
            # This is related to the 'application/problem+json'
            #rfc_7807_compliant_errors: true
            rfc_7807_compliant_errors: true
    use_symfony_listeners: true
    mapping:
        paths:
            - '%kernel.project_dir%/src/Entity'
            - '%kernel.project_dir%/src/WOPI/REST/Request'
