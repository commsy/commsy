knp_snappy:
    pdf:
        enabled:    true
        binary:     '%env(WKHTMLTOPDF_PATH)%'
        options:    []
    # No image renderer is shipped: nothing asks for one, and the binary is as
    # large as wkhtmltopdf itself.
    image:
        enabled:    false
