fos_elastica:
    clients: []
    indexes:
        # Rooms
        commsy_room:
            index_name: '%commsy.elastic.prefix%_room'

            persistence:
                driver: orm
                model: App\Entity\Room
                listener: false