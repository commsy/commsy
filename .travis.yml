language: php
php:
  - 5.6
  - 7.0

services:
  - elasticsearch

addons:
  mariadb: 10.1

install:
  - php composer.phar install
  - cp app/config/paramters.yml.dist app/config/parameters.yml

before_script:
  - sleep 10

script:
  - phpunit

